generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                     String   @id @default(cuid())
  email                  String   @unique
  passwordHash           String
  fullName               String?
  role                   String   @default("user")
  phone                  String?
  region                 String?
  city                   String?
  specificAddress        String?
  profileComplete        Boolean  @default(false)
  notificationPermission String?
  isVerified             Boolean  @default(true)
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt
}

// Generic JSON-backed entities — mirrors the flexible schema Base44 used.
// `data` holds all entity-specific fields; RLS is enforced in application code
// (see server/src/entityConfig.js) rather than at the database layer.
model CartItem {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model CategoryConfig {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model ContactRequest {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model Creator {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model CreatorProductLink {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model CustomerReferral {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

// One per shipped order — assigns a local courier (User.role === 'delivery')
// to collect a package from the carrier who brought it from China, replacing
// the manual Telegram hand-off message (see src/entityConfig.js for RLS).
model DeliveryAssignment {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model Favorite {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model Message {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model Notification {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model Order {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model Product {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model ProductEvent {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model ProductLike {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model ProductShare {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model ReferralLink {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model Review {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model UserBehavior {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

model UserNotification {
  id             String   @id @default(cuid())
  data           Json
  createdById    String?
  createdByEmail String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([createdById])
  @@index([createdByEmail])
  @@index([createdAt])
}

// AI review-insights chat conversations (server/src/routes/agents.js)
model AgentConversation {
  id        String   @id @default(cuid())
  userId    String
  agentName String
  messages  Json     @default("[]")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
