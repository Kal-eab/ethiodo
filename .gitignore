#env
.env
.env.*
# ...except the non-secret production API URL, which the frontend build needs.
!.env.production

# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.claude/settings.local.json
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.vite

# Files exceeding GitHub 100MB limit — keep locally, not in git
Products photo/shoes/IMG_4213.MOV
base44/.app.jsonc

# Database backups — contain customer PII (emails, password hashes, addresses)
ethiodo-data-backup.json
*-backup.json
*.dump
